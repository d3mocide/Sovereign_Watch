# Sovereign Watch – JS8Call Bridge Server dependencies
# Install with: pip3 install -r requirements.txt

# Web framework and async runtime
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# JS8Call TCP API client
# https://pypi.org/project/pyjs8call/
pyjs8call>=0.9.0

# WebSocket support (native KiwiSDR client + uvicorn ws transport)
websockets>=12.0

# KiwiSDR node directory fetch (async HTTP)
aiohttp>=3.9.0
