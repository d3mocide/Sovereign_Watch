# 2026-03-28-release-v0.56.0-smaps-retirement

## Issue
After SMAPS sunset decisions were implemented incrementally, release artifacts still reflected earlier NDBC-focused notes and did not clearly package the final retirement state as a coherent deliverable.

## Solution
Cut release `v0.56.0` centered on full SMAPS runtime retirement and maritime conditions stabilization, with synchronized version metadata, changelog, and release notes.

## Changes
- Updated release metadata:
  - `frontend/package.json` version set to `0.56.0`.
- Updated changelog:
  - Added `0.56.0` section to `CHANGELOG.md` with removed runtime paths, compatibility naming changes, and verification summary.
- Updated release notes:
  - Replaced `RELEASE_NOTES.md` content with `v0.56.0` notes covering API/ingestion/frontend/schema removals and upgrade steps.
- Confirmed runtime parity:
  - Live database check for `\dt *smaps*` returns no SMAPS relations.

## Verification
- Frontend: `cd frontend && pnpm run lint && pnpm run test` (pass, 36 tests)
- Backend API: `cd backend/api && ruff check . && python -m pytest` (pass, 46 tests)
- Infra poller: `cd backend/ingestion/infra_poller && ruff check . && python -m pytest` (pass, 50 tests)
- Database parity: `docker compose exec -T sovereign-timescaledb psql -U postgres -d sovereign_watch -c "\\dt *smaps*"` (no relations)

## Benefits
- Provides a clean release boundary after a complex migration/sunset cycle.
- Aligns operator-facing release artifacts with actual runtime behavior.
- Reduces confusion by documenting the post-SMAPS steady-state explicitly.